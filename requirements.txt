# SAGE — Stabilize, Assess, Govern, Enforce
#
# Mirrors the `[experiments]` extra of pyproject.toml with the same version
# floors, for environments that install from a requirements file. pyproject.toml
# remains the source of truth; keep the two in sync when either changes.
#
#   pip install -r requirements.txt
#   pip install -e ".[experiments]"   # equivalent, and installs the package
#
# No API credentials are needed for the simulated experiments or the test suite.

# ── Core framework ────────────────────────────────────────────────────────────
pydantic>=2.0.0
python-dotenv>=1.0.0
httpx>=0.25.0
pyyaml>=6.0
typing-extensions>=4.5.0

# ── Numerics: CNSR estimation, monitors, and the predictive head ──────────────
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.0.0
pandas>=2.0.0

# ── Figures and LaTeX table generation ────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Model access ──────────────────────────────────────────────────────────────
# litellm drives the `--backend litellm` (hosted API) path; httpx above drives
# the `--backend ollama` (local open-weight) path. Neither is needed for
# `--backend simulator`.
litellm>=1.0.0
openai>=1.0.0
tiktoken>=0.5.0
sentence-transformers>=2.2.0

# ── Agent orchestration (core framework paths) ────────────────────────────────
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-community>=0.2.0
langgraph>=0.2.0
chromadb>=0.4.0

# ── Development and testing: pip install -e ".[dev]" ──────────────────────────
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# pytest-cov>=4.0.0
# black>=23.0.0
# isort>=5.12.0
# mypy>=1.0.0
# ruff>=0.1.0
